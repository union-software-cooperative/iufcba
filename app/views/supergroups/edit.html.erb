<div class="col-sm-8 col-sm-offset-2"/>

  <h1><%= t('.editing', entity: t(".#{@supergroup.class.to_s.downcase}")) %></h1>

  <%= render 'form' %>

  <hr/>
  <%= link_to t('.permanently_delete', entity: t(".#{@supergroup.class.to_s.downcase}")),polymorphic_path(@supergroup), method: :delete, class: "btn btn-danger ", style: "margin: 15px; color: white", data: {confirm: "Are you really really sure?"} %>
</div>
