<% message_html = capture do %>
	<%= render(@message) %>
<% end %> 
<% Secpubsub.publish_to "/messages/new", message_html, person_id: current_person.id, person_handle: current_person.display_name %>

$("#new_message")[0].reset();