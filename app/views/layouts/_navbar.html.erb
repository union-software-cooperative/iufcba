<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">
        <!--<img src="<%=image_path('iuf_logo.png')%>">-->
        <img src="<%=image_path('iuf_brewery_logo.png')%>"/> 
      </a>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if person_signed_in? -%>
          <li>
            <%= link_to t('.agreements'), recs_path %>
          </li>
          <li>
            <%= link_to "#{t('.chat')} <span class='chat-badge badge'>1</span>".html_safe, messages_path %>
          <li>
          <li>
            <%= link_to t('.unions'), unions_path %>
          </li>
          <li>
            <%= link_to t('.companies'), companies_path %>
          </li>
          <li>
            <%= link_to t('.people'), people_path %>
          </li>
          <li class="dropdown">
            <a class="dropdown-toggle" aria-expanded="false" area-haspopup="true" role="button" data-toggle="dropdown" href="#">
              <%=profile_thumb(current_person)%>
              <%=current_person.display_name%>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to t('.my_profile'), edit_person_path(id:current_person.id) %>
              </li>
              <li><%= link_to t('.invite'), new_person_invitation_path %></li>
              <li><%= link_to t('.sign_out'), destroy_person_session_path, :method=>'delete' %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <%= link_to t('.sign_in'), new_person_session_path %>
          </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<div id="message-flash-zone">

</div>
